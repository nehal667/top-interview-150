# Day 40 - Kth Largest Element in an Array
# LeetCode Problem

from typing import List

class Solution:
    def findKthLargest(self, nums: List[int], k: int) -> int:
        # Sort array in descending order
        return sorted(nums, reverse=True)[k - 1]
